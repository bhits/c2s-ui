import { PatientTreatmentProviderPipe } from './patient-treatment-provider.pipe';

describe('PatientTreatmentProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientTreatmentProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
