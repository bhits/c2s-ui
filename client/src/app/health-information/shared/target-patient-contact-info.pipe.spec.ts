import { TargetPatientContactInfoPipe } from './target-patient-contact-info.pipe';

describe('TargetPatientContactInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new TargetPatientContactInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
