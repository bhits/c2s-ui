/* tslint:disable:no-unused-variable */
import {ProviderProjectionNamePipe} from "./provider-projection-name.pipe";

describe('ProviderProjectionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProviderProjectionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
